define(['jquery', 'tapin/util/log'], function(JQuery, Log) {
    return function(elem)
    {
        var _this = this;
        var _elem = null;

        this.addVideo = function(id, lambda)
        {
            player.mute();
        }

        this.removeVideo = function(id)
        {

        }

        this.constructor = function(elem)
        {
            _elem = elem;
        }
        this.constructor(elem);
    }
})